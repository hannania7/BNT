-Gitlab에 해군 파일설명 + DBeaver 테이블 속성 + 테이블 명세서 이 세가지로 DB테이블 익히기
: Gitlab에 해군 파일설명의 input부분에서 sql문인 날짜추출, 시간추출 방법 확인
: .py파일의 실행은 경로에 맞는 파일이 없어서 실행을 못함. 따라서 .py파일 눈으로 해석하고 의미해석 불가능한 것은 구글링함


-python 개발 환경 구축
: visual studio code를 이용하여 .py파일의 실행을 위해 anaconda의 가상환경 만든 후 라이브러리를 다운로드 함


-해군, 시화조력, 검증평가 개괄적인 설명 받음(to 전현지대리님)
: 해군과 검증평가는 예측모델 받음 -> 데이터 전처리 -> output이 nc, json파일 만듬 -> DB에 넣는 것까지 해야함
: 시화조력은 예측모델 받음 -> 데이터 전처리 -> output이 nc, json파일까지 만들고 DB에 넣을 필요는 없음
: 해군 프로젝트에서는 우선 python위주로 코드를 보돼, sql문이 쓰일 때에는 그 db테이블과 속성 확인


-질문 혹은 이상한 것
: autodel은 index갯수가 달라서 오류 뜨는 것 같음 그래서 inner join으로 맞춰줌(질문)
: ColManual.py파일은 경로에 파일이 없어서 경로에 들어가보니 20220302라는 폴더가 없음 -> 만들어야 하는 지(질문)